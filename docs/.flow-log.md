## Decision log — AI-assisted merchant resolution

- 2026-08-11T15:14:59Z — Mode switched to auto per user instruction.
- Q3 resolved: bulk-cleanup tool (CanonicalizeMerchantsPage / POST /merchants/canonicalize) ALSO gets the AI ladder — user's explicit answer: "Also gets the AI layer".
- Implementation decision (auto-approved, not re-asked): bulk-cleanup reuses the SAME /merchants/resolve ladder (corrections -> history -> AI) rather than a second bespoke pathway, for consistency and to avoid duplicate logic. The history-check step is NOT skipped for bulk-cleanup — it still provides a valid free win whenever a regex guess already matches an established clean name in the user's data (this is not the same as matching messy data; it matches the CLEANED candidate against real history).
