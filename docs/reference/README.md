> **Status:** Live · **Last verified:** 2026-09-16

# Reference

How Daybook is actually built — the material a session needs to look up rather
than to obey. The rules and the hard-won traps stay in [`../../CLAUDE.md`](../../CLAUDE.md);
this folder holds the parts that are long, stable and only needed on demand.

_(Populated in PR 2, which moves the schema, architecture and folder map out of
CLAUDE.md.)_
